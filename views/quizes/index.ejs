<table>
	<tr>
		<form name="busqueda" method="get" action="">
		<td><input type="text" name="search" value="">
		</td>
		<td><button type="submit">Buscar</button>
		</td>
		</form>
	</tr>
	<tr><td></td><td></td></tr>
<% var i; for (i=0; i < quizes.length; i++) { %>
	<tr>
		<td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %> (<%= quizes[i].tema %>)</a></td>
		<% if (session.user) { %>
			<td><a href="quizes/<%= quizes[i].id %>/edit"><button>Editar</button></a></td>
			<td>
				<form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
					<button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
						Borrar
					</button>
				</form>
			</td>
		<% } %>
	</tr>
<% } %>
<% if(session.user){ %>
	<tr>
		<td><a href="/quizes/new"><button type="button">Crear pregnta</button></a></td>
	</tr>
<% } %>		
</table>